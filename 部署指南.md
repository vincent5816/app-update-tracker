# App 更新追踪面板 · 部署指南

## 项目文件结构

上传到 GitHub 后，目录应该是这样的：

```
app-update-tracker/
├── index.html
├── package.json
├── vite.config.js
└── src/
    ├── main.jsx
    └── App.jsx        ← 主要数据在这里
```

---

## 第一步：在 GitHub 创建仓库

1. 打开 https://github.com/new
2. Repository name 填写：`app-update-tracker`
3. 选择 **Public**（Vercel 免费版需要公开仓库）
4. **不要**勾选 "Add a README file"
5. 点击 **Create repository**

---

## 第二步：上传文件

进入刚创建的仓库，点击 **uploading an existing file**，然后：

1. 把 `index.html`、`package.json`、`vite.config.js` 直接拖进去
2. 点击 **Commit changes**

接着创建 src 文件夹：
1. 点击 **Add file → Create new file**
2. 文件名输入：`src/main.jsx`（输入斜杠会自动创建文件夹）
3. 把 main.jsx 的内容粘贴进去，点击 **Commit changes**
4. 同样方式创建 `src/App.jsx`，粘贴内容，Commit

---

## 第三步：部署到 Vercel

1. 打开 https://vercel.com，用 GitHub 账号登录
2. 点击 **Add New → Project**
3. 找到 `app-update-tracker` 仓库，点击 **Import**
4. Framework Preset 选择 **Vite**
5. 其他保持默认，点击 **Deploy**
6. 等待约 1 分钟，部署完成后会得到一个网址

---

## 日常更新数据

每次让 Claude 帮你抓取新内容后，只需要：

1. 打开 GitHub 仓库，点击 `src/App.jsx`
2. 点击右上角铅笔图标（Edit）
3. 把 Claude 给你的新版 App.jsx 内容全部替换粘贴进去
4. 点击 **Commit changes**
5. Vercel 自动重新部署，约 30 秒后网址内容更新

---

## 提示

- **数据存储**：`INITIAL_APPS` 里的数据在代码里，你通过 GitHub 更新
- **用户自添加的 App**：保存在浏览器 localStorage，换设备会丢失
- **更新数据**：告诉 Claude「帮我更新星野最新动态」，Claude 会给你新版 App.jsx
